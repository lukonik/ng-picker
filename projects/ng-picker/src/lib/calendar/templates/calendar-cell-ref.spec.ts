import { CalendarCellRef } from './calendar-cell-ref';

describe('CalendarCellRef', () => {
  it('should create an instance', () => {
    const directive = new CalendarCellRef();
    expect(directive).toBeTruthy();
  });
});
