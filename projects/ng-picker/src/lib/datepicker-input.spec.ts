import { DatepickerInput } from './datepicker-input';

describe('DatepickerInput', () => {
  it('should create an instance', () => {
    const directive = new DatepickerInput();
    expect(directive).toBeTruthy();
  });
});
